apiVersion: apps/v1
kind: Deployment
metadata:
  name: pay-rpc
  namespace: go-zero-shop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pay-rpc
  template:
    metadata:
      labels:
        app: pay-rpc
    spec:
      initContainers:
      - name: wait-for-deps
        image: busybox:1.36
        command: ['sh', '-c', 'until nc -z mysql 3306 && nc -z redis 6379 && nc -z etcd 2379; do echo waiting for mysql/redis/etcd; sleep 2; done']
      containers:
      - name: pay-rpc
        image: go-zero-shop/pay-rpc:latest
        imagePullPolicy: IfNotPresent
        command: ["/app/app-binary", "-f", "/app/etc/pay.yaml"]
        ports:
        - containerPort: 9101
          name: metrics
        - containerPort: 8085
          name: grpc
        volumeMounts:
        - name: config
          mountPath: /app/etc
          readOnly: true
        startupProbe:
          grpc:
            port: 8085
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 30
        readinessProbe:
          grpc:
            port: 8085
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 6
        livenessProbe:
          grpc:
            port: 8085
          initialDelaySeconds: 30
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 6
      volumes:
      - name: config
        configMap:
          name: pay-rpc-config
---
apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/metrics"
    prometheus.io/port: "9101"
  name: pay-rpc
  namespace: go-zero-shop
spec:
  selector:
    app: pay-rpc
  ports:
  - port: 8085
    targetPort: 8085
    name: grpc
  - port: 9101
    targetPort: 9101
    name: metrics
  type: ClusterIP
