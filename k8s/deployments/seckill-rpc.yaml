apiVersion: apps/v1
kind: Deployment
metadata:
  name: seckill-rpc
  namespace: go-zero-shop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: seckill-rpc
  template:
    metadata:
      labels:
        app: seckill-rpc
    spec:
      initContainers:
      - name: wait-for-deps
        image: busybox:1.36
        command: ['sh', '-c', 'until nc -z redis 6379 && nc -z etcd 2379 && nc -z kafka 9092 && nc -z product-rpc 8081; do echo waiting for redis/etcd/kafka/product-rpc; sleep 2; done']
      containers:
      - name: seckill-rpc
        image: go-zero-shop/seckill-rpc:latest
        imagePullPolicy: IfNotPresent
        command: ["/app/app-binary", "-f", "/app/etc/seckill.yaml"]
        ports:
        - containerPort: 9101
          name: metrics
        - containerPort: 9889
          name: grpc
        volumeMounts:
        - name: config
          mountPath: /app/etc
          readOnly: true
        startupProbe:
          grpc:
            port: 9889
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 30
        readinessProbe:
          grpc:
            port: 9889
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 6
        livenessProbe:
          grpc:
            port: 9889
          initialDelaySeconds: 30
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 6
      volumes:
      - name: config
        configMap:
          name: seckill-rpc-config
---
apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/metrics"
    prometheus.io/port: "9101"
  name: seckill-rpc
  namespace: go-zero-shop
spec:
  selector:
    app: seckill-rpc
  ports:
  - port: 9889
    targetPort: 9889
    name: grpc
  - port: 9101
    targetPort: 9101
    name: metrics
  type: ClusterIP
