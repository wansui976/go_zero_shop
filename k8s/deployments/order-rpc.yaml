apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-rpc
  namespace: go-zero-shop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-rpc
  template:
    metadata:
      labels:
        app: order-rpc
    spec:
      initContainers:
      - name: wait-for-deps
        image: busybox:1.36
        command: ['sh', '-c', 'until nc -z mysql 3306 && nc -z redis 6379 && nc -z etcd 2379 && nc -z user-rpc 8080 && nc -z product-rpc 8081 && nc -z pay-rpc 8085 && nc -z rabbitmq 5672 && nc -z dtm 36790; do echo waiting for dependencies; sleep 2; done']
      containers:
      - name: order-rpc
        image: go-zero-shop/order-rpc:latest
        imagePullPolicy: IfNotPresent
        command: ["/app/app-binary", "-f", "/app/etc/order.yaml"]
        ports:
        - containerPort: 9101
          name: metrics
        - containerPort: 8082
          name: grpc
        volumeMounts:
        - name: config
          mountPath: /app/etc
          readOnly: true
        startupProbe:
          grpc:
            port: 8082
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 30
        readinessProbe:
          grpc:
            port: 8082
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 6
        livenessProbe:
          grpc:
            port: 8082
          initialDelaySeconds: 30
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 6
      volumes:
      - name: config
        configMap:
          name: order-rpc-config
---
apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/metrics"
    prometheus.io/port: "9101"
  name: order-rpc
  namespace: go-zero-shop
spec:
  selector:
    app: order-rpc
  ports:
  - port: 8082
    targetPort: 8082
    name: grpc
  - port: 9101
    targetPort: 9101
    name: metrics
  type: ClusterIP
