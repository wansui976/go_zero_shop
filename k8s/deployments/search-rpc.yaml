apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-rpc
  namespace: go-zero-shop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: search-rpc
  template:
    metadata:
      labels:
        app: search-rpc
    spec:
      initContainers:
      - name: wait-for-deps
        image: busybox:1.36
        command: ['sh', '-c', 'until nc -z elasticsearch 9200 && nc -z redis 6379 && nc -z etcd 2379 && nc -z product-rpc 8081; do echo waiting for elasticsearch/redis/etcd/product-rpc; sleep 2; done']
      containers:
      - name: search-rpc
        image: go-zero-shop/search-rpc:latest
        imagePullPolicy: IfNotPresent
        command: ["/app/app-binary", "-f", "/app/etc/search.yaml"]
        ports:
        - containerPort: 9101
          name: metrics
        - containerPort: 8083
          name: grpc
        volumeMounts:
        - name: config
          mountPath: /app/etc
          readOnly: true
        startupProbe:
          grpc:
            port: 8083
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 30
        readinessProbe:
          grpc:
            port: 8083
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 6
        livenessProbe:
          grpc:
            port: 8083
          initialDelaySeconds: 30
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 6
      volumes:
      - name: config
        configMap:
          name: search-rpc-config
---
apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/metrics"
    prometheus.io/port: "9101"
  name: search-rpc
  namespace: go-zero-shop
spec:
  selector:
    app: search-rpc
  ports:
  - port: 8083
    targetPort: 8083
    name: grpc
  - port: 9101
    targetPort: 9101
    name: metrics
  type: ClusterIP
