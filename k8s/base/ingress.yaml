# /tmp/go_zero_shop_extracted/go_zero_shop/deploy/kubernetes/base/ingress.yaml

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shop-ingress
  namespace: go-zero-shop
  # 注解 (Annotations)：用于配置 Nginx Ingress Controller 的高级特性
  annotations:
    # 路径重写：将请求路径重定向到后端服务的根路径
    nginx.ingress.kubernetes.io/rewrite-target: /
    # 限制上传文件的大小为 50MB
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    # 后端服务响应超时时间 (秒)
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "60"
    # 开启跨域资源共享 (CORS)，允许前端网页跨域请求
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/cors-allow-methods: "GET, PUT, POST, DELETE, PATCH, OPTIONS"
    nginx.ingress.kubernetes.io/cors-allow-headers: "DNT,X-CustomHeader,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Authorization"
    # 安全与防护：限流配置
    # 限制每个 IP 每秒请求数为 100 (RPS)
    nginx.ingress.kubernetes.io/limit-rps: "100"
    # 限制单个 IP 的并发连接数为 50
    nginx.ingress.kubernetes.io/limit-connections: "50"
spec:
  # 指定使用的 Ingress 控制器类名
  ingressClassName: nginx
  rules:
    # 匹配访问域名
    - host: api.shop.local
      http:
        paths:
          # 路由规则 1：访问 api.shop.local/user/* 转发到用户中心
          - path: /user
            pathType: Prefix
            backend:
              service:
                name: user-api-service
                port:
                  number: 8888
          # 路由规则 2：访问 api.shop.local/product/* 转发到商品中心
          - path: /product
            pathType: Prefix
            backend:
              service:
                name: product-api-service
                port:
                  number: 8889
          # 路由规则 3：访问 api.shop.local/order/* 转发到订单中心
          - path: /order
            pathType: Prefix
            backend:
              service:
                name: order-api-service
                port:
                  number: 8890
          # 路由规则 4：访问 api.shop.local/seckill/* 转发到秒杀业务
          - path: /seckill
            pathType: Prefix
            backend:
              service:
                name: seckill-api-service
                port:
                  number: 8891
  # HTTPS 配置
  tls:
    - hosts:
        - api.shop.local
      # 证书存储在名为 shop-tls-secret 的 Secret 中
      secretName: shop-tls-secret